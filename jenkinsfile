pipeline {
  agent {
    docker {
      image 'node:8.9.3'
      args '--rm -d --name "nodejs" -it -v $HOME:/home'
    }
  }
  environment {
    CI = 'true'
  }
  stages {
    stage('Build') {
      steps {
        sh 'npm install'
      }
    }
    stage('Test') {
      when {
        branch 'develop'
      }
      steps {
        sh 'ddd'
        stash includes: '**', name: 'App'
      }
    }
    stage('Develop') {
      when {
        branch 'develop'
      }
      steps {
        echo 'Develop Environment'
      }
    }
  }
}
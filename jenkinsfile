node {
  environment {
    CI = 'true'
  }
  stage('Develop') {
    when {
      branch 'develop'
    }
    // docker connection
    sh 'sudo docker exec -i develop bash'
    // sleep for connetion delay
    sleep 1
    // 
    echo "develop docker"
    // git checkout
    checkout scm
  }
}